# arthouse-ops environment scaffold (initial).
# Copy this file to .env and fill in real values. The full, commented list of
# every variable lands on the feat/dashboard-mvp branch.
#
#   cp .env.example .env
#
# Never commit the real .env. It is gitignored.

BACKFILL_MODE=true
