# Pinned to major versions. The pipeline runs weekly and unattended, so a
# surprise from a minor release should show up in a pull request, not in a
# Sunday morning run.
anthropic>=1.3,<2
requests>=2.31,<3
google-api-python-client>=2.120,<3
google-auth>=2.28,<3
python-dotenv>=1.0,<2
